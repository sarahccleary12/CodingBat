/*
PROMPT
Given 2 int values, return true if either of them is
in the range 10..20 inclusive.
*/

public boolean in1020(int a, int b) {
  return (((a > 9) && (a < 21)) || ((b > 9) && (b < 21)));
}

/*
Check to see if either a OR b is within the range 10 to 20,
inclusive. Line 8 will return true if either of them is in
the correct range.
*/
